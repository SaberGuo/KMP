<Window x:Class="KMP.Reporter.ReportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        xmlns:local="clr-namespace:KMP.Reporter"
        Closing="Window_Closing"
        mc:Ignorable="d"
        Title="报告生成">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="600"/>
        </Grid.RowDefinitions>
        <ToolBar Grid.Row="0">
            <Button Command="{Binding DocGenerateCommand}">报告生成</Button>
        </ToolBar>
        <local:DefaultDocViewer x:Name="docviewer" Grid.Row="1" DocumentPath="{Binding DocPath}"/>

    </Grid>
    
</Window>
